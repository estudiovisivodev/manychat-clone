import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Note: In Next.js 16+, instrumentation.ts is automatically detected
  // and the instrumentation hook is enabled by default.
  // The experimental flag is not required.
};

export default nextConfig;
